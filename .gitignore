/.transfer/
__pycache__/
*.py[cod]
.control-token
.env
.env.*
!.env.example

# Vitis HLS / Vivado local build artifacts (regenerated by run_hls.tcl /
# vivado/*.tcl - HW/vitis_hls.log is the one deliberately-committed
# historical run log and stays tracked; this only ignores NEW stray copies
# and the full project output trees).
*_prj/
vitis_hls.log
*.jou
.Xil/
webtalk/
*.wdb
*.pb
.local.xsc.settings.verify
